module.exports = (client, message) => {
    /*log for deleted messages I don't need this but still interesting
    if (message.author.bot) return;

    console.log(`LOG: (${time()}) (${message.guild.name}) DELETED MESSAGE FROM ${message.member.user.tag}: ${message}`);
    */
}
/*
function time() {
    let str = "";
    let currentTime = new Date()
    let hours = currentTime.getHours()
    let minutes = currentTime.getMinutes()
    let seconds = currentTime.getSeconds()
  
    if (minutes < 10) {
        minutes = "0" + minutes
    }
    if (seconds < 10) {
      seconds = "0" + seconds
    }
    str += hours + ":" + minutes + ":" + seconds + " " ;
    if(hours > 11){
        str += "PM"
    } else {
        str += "AM"
    }
    return str;
}*/